# Минимальный .htaccess для работы маршрутов /article/...
# Безопасная версия — не вызывает ошибок 500

# Включаем режим перезаписи ТОЛЬКО если модуль доступен
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Не применять правила к существующим файлам
    RewriteCond %{REQUEST_FILENAME} !-f
    
    # Все остальные запросы перенаправляем на index.html
    RewriteRule ^ index.html [L]
</IfModule>

# Запрещаем листинг директорий
Options -Indexes